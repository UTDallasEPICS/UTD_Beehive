import "../styles/graphContainer.css";

function GraphContainer(props) {
  const { data } = props;

  return <div className="graph">{/* render corresponding graph */}</div>;
}

export default GraphContainer;

/* 

Ask them the outline for the JSON file with the data inside it:

{
    // populate it
}


*/
